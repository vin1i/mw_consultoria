<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Título padrão do imóvel' %></title>

  <!-- Open Graph meta tags para redes sociais -->
  <meta property="og:title" content="<%= title || 'Título padrão do imóvel' %>">
  <meta property="og:description" content="<%= description || 'Descrição padrão do imóvel.' %>">
  <meta property="og:image" content="<%= images.length > 0 ? images[0] : 'default-image-url.jpg' %>">
  <meta property="og:url" content="<%= url || 'https://mwconsultoriaimobiliaria.com.br' %>">
  <meta property="og:type" content="website">
</head>
<body>
  <h1><%= title || 'Título padrão do imóvel' %></h1>
  <p><%= description || 'Descrição padrão do imóvel.' %></p>
  
  <h2>Imagem principal do imóvel:</h2>
  <div>
    <% if (images.length > 0) { %>
      <img src="<%= images[0] %>" alt="Imagem do imóvel" style="max-width: 100%; height: auto;">
    <% } else { %>
      <p>Não há imagens disponíveis para este imóvel.</p>
    <% } %>
  </div>

  <p><a href="<%= url || 'https://mwconsultoriaimobiliaria.com.br' %>">Ver mais detalhes</a></p>
</body>
</html>
